# Ask the user for width and height for Calculate the area
width = float(input("Enter the width of the rectangle: "))
height = float(input("Enter the height of the rectangle: "))

# Calculate the area
area = width * height

# Display the result
print("The area of the rectangle is:->", area)
